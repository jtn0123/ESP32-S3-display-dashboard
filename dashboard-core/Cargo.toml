[package]
name = "dashboard-core"
version = "0.1.0"
edition = "2021"

[dependencies]
# Core dependencies that work on host
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
# Test dependencies
proptest = "1.0"
quickcheck = "1.0"

[features]
default = []