[package]
name = "esp32-s3-dashboard"
version = "0.1.0"
edition = "2021"

[dependencies]
# Just use core dependencies to test compilation
heapless = "0.8"
libm = "0.2"

[lib]
name = "rust_dashboard"
path = "src/lib.rs"