# Python requirements for OTA tools
# Install with: pip install -r requirements-ota.txt

# Core requirements
requests>=2.28.0       # HTTP requests for OTA uploads

# Optional but recommended for auto-discovery
zeroconf>=0.39.0      # mDNS discovery support
netifaces>=0.11.0     # Network interface detection

# Development tools (optional)
python-dotenv>=0.19.0  # Environment variable management